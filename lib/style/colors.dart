import 'package:flutter/material.dart';

abstract class ThemeColors {
  // AppBar Colors
  static const Color homePageAppbar = Color(0xff5b0060);

  // Divider Colors
  static final Color customDivider = const Color(0xff5b0060).withOpacity(0.2);

  //Swipe Backgrond Colors
  static const Color swipeBackground = Colors.red;
}
